{
  "name": "ousemg-interface",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File scripts/dev.ps1",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "python -m uvicorn main:app --reload --app-dir modules/atlas/backend",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "data:alumni": "python modules/professional-development/scripts/convert_alumni.py"
  }
}
