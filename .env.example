# Logo.dev sample URL — token is parsed from the query string.
# Replace the domain with any firm domain when testing; the app builds URLs dynamically.
LOGO_API=https://img.logo.dev/stripe.com?token=YOUR_TOKEN_HERE
